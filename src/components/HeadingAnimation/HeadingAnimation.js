import React from 'react'

export default class HeadingAnimation extends React.Component {

  render() {
    return(
      <h2>
        {/*
          TODO: Add functionality to use this component in UnderConstruction.
          It should accept 'string' as props and use it to return animated text
        */}
      </h2>
    )
  }
}